import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:consultoria/app/modules/tasks/tasks_page.dart';

void main() {
  testWidgets('TasksPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TasksPage(title: 'Tasks')));
    //  final titleFinder = find.text('Tasks');
    //  expect(titleFinder, findsOneWidget);
  });
}
